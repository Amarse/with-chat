import React from 'react';
import { useAuth } from 'context/user.context';

const Group = (): JSX.Element => {
  return (
    <div className='w-screen h-screen'>
      <h2 className='p-4'>그룹 채팅</h2>
    </div>
  );
};

export default Group;
