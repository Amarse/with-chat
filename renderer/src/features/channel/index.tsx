import { auth } from 'Fbase';
import { useRouter } from 'next/router';
import { useAuth } from 'context/user.context';

const Channel = ({ id }): JSX.Element => {
  console.log(id);
  return <></>;
};

export default Channel;
